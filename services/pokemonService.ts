
function getAllOwnedPokemon (userid: string) {
  console.log(userid);
  return [];
}

function getOneOwnedPokemon(userId: string, pokedexNumber: number) {
  console.log(userId, pokedexNumber);
  return [];
}

export default {
  getAllOwnedPokemon,
  getOneOwnedPokemon,
};